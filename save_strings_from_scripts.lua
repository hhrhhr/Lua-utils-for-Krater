function SaveStringsFromScripts()
    print("[ log] saving strings used by scripts")
    
    local fin = "work/strings_in_scripts.txt"
    local fout = "gui.txt"
    local ft = assert(io.open(fout, "w+"))

    for line in io.lines(fin) do

    end
    

end
